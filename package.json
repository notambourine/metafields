{
  "name": "@notambourine/metafields",
  "version": "0.0.8",
  "description": "Create-only Shopify custom-data schema CLI",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/notambourine/metafields.git"
  },
  "bugs": {
    "url": "https://github.com/notambourine/metafields/issues"
  },
  "homepage": "https://github.com/notambourine/metafields#readme",
  "engines": {
    "node": ">=22.18"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "metafields": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "npm run build && tsc -p test/tsconfig.json --noEmit && tsc -p test-d/tsconfig.json --noEmit && tsc -p scripts/tsconfig.json --noEmit",
    "generate:metafield-types": "node scripts/generate-metafield-types.ts",
    "test": "npm run build && node --test test/*.test.ts",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
